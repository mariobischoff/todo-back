module.exports = app => {
  return {
    save: () => {
      // verificar password com password2
      // verificar campos obrigatorios
      // hash do password
      // importar model
    },
    update: () => {
      // verificar campos obrigatorios
      // verificar parametro
      // verificar token
      // importar o model
    },
    list: () => {
      // verificar se foi passado parametro
      // verificar token
      // importar model
    },
    delete: () => {
      // verificar parametro
      // verificar token
      // importar model
    }
  }
}